# Use an official Debian image as the base
FROM debian:latest

# Set the init system to "dumb-init"
RUN apt-get update && apt-get install -y dumb-init
ENTRYPOINT ["dumb-init", "--"]

# Create a new user named "marc"
RUN useradd marc

# Switch to the "marc" user
USER marc
