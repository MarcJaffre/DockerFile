FROM debian:bookworm
RUN apt update && apt upgrade -y
RUN apt install -y systemd
RUN apt install -y systemd-container
RUN useradd marc -s /usr/bin/bash
USER marc
CMD ["/usr/bin/systemd-nspawn", "--boot", "--directory=/", "--user=1000"]
