# Use Debian as the base image
FROM debian:latest

# Set the environment variable
ENV container=docker

# Update the package list
RUN apt update

# Install systemd and dumb-init
RUN apt install -y systemd dumb-init

# Expose the command to run systemd with dumb-init
CMD ["dumb-init", "--", "systemd"]
