# Use Debian as the base image
FROM debian:latest

# Set the environment variable
ENV container=docker

# Update the package list
RUN apt update

# Install systemd
RUN apt install -y systemd

# Expose the command to run systemd
CMD ["systemd"]
